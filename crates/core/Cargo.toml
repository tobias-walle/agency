[package]
name = "orchestra-core"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { version = "0.4.41", features = ["serde"] }
regex = "1.11.2"
serde = { version = "1.0.219", features = ["derive"] }
serde_yaml = "0.9.34"
thiserror = "2.0.16"

[dev-dependencies]
proptest = "1.7.0"
