## 1. CLI behavior and tests

- [x] 1.1 Add or update CLI spec for `agency new` default, draft, and no-attach behavior
- [x] 1.2 Implement updated `Commands::New` behavior in `crates/agency/src/lib.rs`
- [x] 1.3 Ensure `commands::new::run` behavior stays compatible with TUI usage
- [x] 1.4 Add integration tests in `crates/agency/tests/cli_new.rs` for new, draft, and no-attach flows
- [x] 1.5 Run `just check`, `just test`, and `just check-strict` and fix any issues
