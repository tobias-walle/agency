# Feedback Collection

- [ ] Add more examples to `agency --help`.
- [ ] Improve error message when running `agency new` outside a git repository.
- [ ] Add `editor = ["cmd"]` config option.
- [ ] Autostart daemon on every command.
- [ ] Rename `agency ps` to `agency tasks` for clarity.
- [ ] Prevent Agency from failing without an initial commit.
- [ ] Make `agency config` open the config JSON file.
- [ ] Show unstaged changes and number of commits against base branch
- [ ] Allow creation of base branch. If selected branch doesn't exists, create a new one
- [ ] Investigate C-z
- [ ] Add --follow commands
